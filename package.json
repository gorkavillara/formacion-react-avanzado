{
  "name": "proyecto-esbuild",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "esbuild src/index.js --bundle --outfile=dist/index.js --loader:.js=jsx --format=cjs",
    "dev": "esbuild src/index.js --bundle --outfile=dist/index.js --loader:.js=jsx --format=cjs --watch --servedir=dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.5"
  }
}
